@extends('layouts.admin') @section('title',$title) @section('content')<h1 class="text-3xl font-black text-brand-900">{{ $title }}</h1><form method="post" action="{{ $item->exists ? route('admin.'.$route.'.update',$item) : route('admin.'.$route.'.store') }}" class="mt-7 max-w-3xl rounded-2xl bg-white p-7 shadow-sm">@csrf @if($item->exists)@method('PUT')@endif<div class="grid gap-5">@foreach($fields as $name=>$field)@php($value=old($name,$item->$name ?? ($field['default']??'')))@if($field['type']==='checkbox')<label class="flex items-center gap-3 font-bold"><input type="checkbox" name="{{ $name }}" value="1" @checked($value) class="size-5">{{ $field['label'] }}</label>@else<label class="text-sm font-bold">{{ $field['label'] }}@if($field['type']==='textarea')<textarea name="{{ $name }}" rows="{{ $name==='content'||$name==='description' ? 8:3 }}" class="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 font-normal outline-none focus:border-brand-500">{{ $value }}</textarea>@else<input type="{{ $field['type'] }}" name="{{ $name }}" value="{{ $value }}" class="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 font-normal outline-none focus:border-brand-500">@endif</label>@endif @endforeach</div><div class="mt-7 flex gap-3"><button class="rounded-xl bg-brand-600 px-6 py-3 font-bold text-white">Lưu</button><a href="{{ route('admin.'.$route.'.index') }}" class="rounded-xl bg-slate-100 px-6 py-3 font-bold">Hủy</a></div></form>@endsection
