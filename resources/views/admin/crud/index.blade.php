@extends('layouts.admin') @section('title',$title) @section('content')<div class="flex items-center justify-between"><h1 class="text-3xl font-black text-brand-900">{{ $title }}</h1>@if($canCreate ?? true)<a href="{{ route('admin.'.$route.'.create') }}" class="rounded-xl bg-brand-600 px-5 py-3 text-sm font-bold text-white">Thêm mới</a>@endif</div><div class="mt-7 overflow-x-auto rounded-2xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b bg-slate-50 text-xs uppercase text-slate-500"><tr>@foreach($columns as $label)<th class="px-5 py-4">{{ $label }}</th>@endforeach<th class="px-5 py-4 text-right">Thao tác</th></tr></thead><tbody>@forelse($items as $item)<tr class="border-b border-slate-100">@foreach($columns as $key=>$label)<td class="px-5 py-4">@if($key==='is_active')<span class="rounded-full px-2 py-1 text-xs {{ $item->$key ? 'bg-emerald-100 text-emerald-700':'bg-slate-100 text-slate-500' }}">{{ $item->$key ? 'Bật':'Tắt' }}</span>@elseif($key==='base_price'){{ number_format((float)$item->$key,2,',','.') }}@else{{ Str::limit($item->$key,70) }}@endif</td>@endforeach<td class="px-5 py-4 text-right"><a class="font-bold text-brand-600" href="{{ route('admin.'.$route.'.edit',$item) }}">Sửa</a>@if($canDelete ?? true)<form class="ml-3 inline" method="post" action="{{ route('admin.'.$route.'.destroy',$item) }}" onsubmit="return confirm('Xóa dữ liệu này?')">@csrf @method('DELETE')<button class="text-red-600">Xóa</button></form>@endif</td></tr>@empty<tr><td colspan="{{ count($columns)+1 }}" class="px-5 py-10 text-center text-slate-400">Chưa có dữ liệu.</td></tr>@endforelse</tbody></table></div>@if(method_exists($items,'links'))<div class="mt-6">{{ $items->links() }}</div>@endif @endsection
